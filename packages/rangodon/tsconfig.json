{
    "include": ["src"],
    "extends": "../tsconfig.base.json",
    "compilerOptions": {
        // to override config from tsconfig.base.json
        "outDir": "dist",
        "rootDir": ".",
        // for references
        "baseUrl": "."
    },
    // references required for monorepo to work
    "references": [{ "path": "../utilidon" }]
}
